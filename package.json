{
  "name": "podarok-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "cd podarokplaner/webapp && npm install && npm run build",
    "postinstall": "cd podarokplaner/webapp && npm install"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "serverless-http": "^3.2.0"
  }
}
